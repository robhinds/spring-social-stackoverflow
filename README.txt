This is an implementation of the Spring-Social projects for StackOverflow.

At the moment it is a very early work in progress and only currently supports the basic operation of returning an authenticated user.

Needs to be extended to support Question/Answer/Tags/Badges API as well.